mixin createHeaderItem(ref, title)
    a(href=ref)
        h3 #{title}

-
    var headerElems = [
        {
            "ref": "/",
            "title": "Главная"
        },
        {
            "ref": "/table.html",
            "title": "Сводка акций"
        },
        {
            "ref": "/about.html",
            "title": "Информация"
        }
    ]

.searchBlock
    input
    h 🔍
.header
    .wrapper
        each elem in headerElems
            +createHeaderItem(elem.ref, elem.title)

    h.title Акции